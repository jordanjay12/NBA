// This page has been connnected to the footer

// check if the date exists on the page somewhere - means we are on the home page

// if date on page use it, else use moment timezone and moment.js - We need to use it so that we know what day is currently being presented to the user

var tomorrow = document.getElementById("nextDayButton");
var yesterday = document.getElementById("prevDayButton");